# V24 — Doctor Page Image Update

- Replaced the doctor hero image on `doctor-profile.html` with the newly provided Visage clinic photograph.
- Added the image as `assets/doctor-neeraja-visage.png`.
- Existing responsive layout and image `object-fit: cover` behavior are preserved so the image fills the doctor photo area.
